# Copy to .env and fill in. The .env file is gitignored.
# Real environment variables (exported in your shell) take precedence over .env.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=leonid
DB_USER=leonid
DB_PASSWORD=changeme

# Optional connection-pool tuning
DB_POOL_MAX_SIZE=10
